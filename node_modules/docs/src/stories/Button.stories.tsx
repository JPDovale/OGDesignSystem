import type { StoryObj, Meta } from '@storybook/react'
import { Button, ButtonProps } from '@og-ui/react'

export default {
  title: 'Button OG',
  component: Button,

  args: {
    children: 'Enviar',
  },
} as Meta<ButtonProps>

export const Default: StoryObj<ButtonProps> = {}

export const Focus: StoryObj<ButtonProps> = {
  args: {
    variant: 'focus',
  },
}

export const Active: StoryObj<ButtonProps> = {
  args: {
    variant: 'active',
  },
}
